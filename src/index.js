import React from 'react';
import ReactDom from 'react-dom/client';
import RoutingComponent from './components/App-Routing';

const root = ReactDom.createRoot(document.getElementById('root'));
root.render(<RoutingComponent />);
